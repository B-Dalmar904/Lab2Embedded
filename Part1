#include <msp430.h> 

float b = 5;

/**
 * main.c
 */
int main(void)
{
	WDTCTL = WDTPW | WDTHOLD;	// stop watchdog timer
	
	int a = 3;
	unsigned char c = 0x0f;
	unsigned char d = 0x05;

	unsigned char f = c||d;
	unsigned char g = c&&d;

	a += 1;
	b -= 1;




	return 0;
}
